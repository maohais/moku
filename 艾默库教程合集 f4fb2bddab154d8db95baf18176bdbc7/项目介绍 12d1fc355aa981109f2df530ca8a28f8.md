# 项目介绍

```markdown
## 聚合脚本（polymerization）
文件夹“polymerization”内的所有脚本统称为聚合脚本，运行UPDATE.js即可自动生成表格及配置内容。  
兼容青龙脚本，可直接在青龙内下运行  

### 聚合脚本优势
**金山文档**
* 所有脚本及配置表格汇集在一个文档中，利于统一管理和配置
* 方便后续更新脚本，仅需运行UPDATE脚本即可自动新增最新表格及配置，不再需要手动新建表格框架
* 方便定时任务的添加与查看
* 支持仅推送错误消息、推送昵称等，支持更多的推送方式
* 配置灵活快捷，利于新增脚本及新配置功能
* 支持多脚本共用同一个表格，如WPS(轻量版)、WPS(客户端版)、WPS(稻壳版)脚本共用名称为wps的表格。  
**青龙**  
* ql开头的脚本也可以直接在青龙中运行  

## 金山文档建议  
* 定时任务不设在同一时间  

```

# 艾默库适配更新日志：

```jsx
// 艾默库适配
// v2.5.0   兼容@和换行cookie适配
// v2.4.0
// v2.2.0   增加cookie转义字符解析，将用户内容含的分隔符解析为正常字符
// v2.1.0   增加响应headers，使能从响应直接读取响应headers
// v2.0.0   对多用户的并发处理进行优化，使其变成串行处理
// v1+      同时响应text()和json()函数，使得能够适配金山响应的结果处理
// v1.1.0   增加细微cookie功能，使得能够对同一用户传入多值进行处理
// v1.0.0   增加青龙适配

```